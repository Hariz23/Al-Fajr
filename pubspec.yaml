name: al_fajr
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_qiblah: ^3.2.0
  geolocator: ^14.0.2 # Standard stable version for 2026
  permission_handler: ^12.0.1
  http: ^1.2.0
  islamic_hijri_calendar: ^0.0.4
  hijri: ^3.0.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_core: ^4.4.0
  intl: ^0.19.0
  url_launcher: ^6.3.2
  provider: ^6.1.5+1
  shared_preferences: ^2.0.15
  # THE NOTIFICATION FIX:
  flutter_local_notifications: ^21.0.0-dev.2
  timezone: ^0.11.0 
  flutter_timezone: ^5.0.1 # Replaced the old "native_timezone"
  # Audio tools for Quran
  just_audio: ^0.10.5
  just_audio_background: ^0.0.1-beta.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
